import { ShowedDirective } from './showed.directive';

describe('ShowedDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowedDirective();
    expect(directive).toBeTruthy();
  });
});
